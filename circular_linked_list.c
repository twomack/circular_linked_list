#include <stdio.h>
#include "circular_linked_list.h"

/* structs */
struct Node {
    int data;
    struct Node *next;
};

/* functions */
void print_list(struct Node *last) {
    printf("Need to implement print_list.\n");
}

void push(struct Node *last_ref, int data) {
    printf("Need to implement push.\n");
}

int pop(struct Node *last_ref, int data) {
    printf("Need to implement pop.\n");
    return 1;
}

void add_begin(struct Node *last_ref, int data) {
    printf("Need to implement add_begin.\n");
}

void add_end(struct Node *last_ref, int data) {
    printf("Need to implement add_end.\n");
}

void add_after(struct Node *last_ref, int data) {
    printf("Need to implement add_after.\n");
}

void add_before(struct Node *last_ref, int data) {
    printf("Need to implement add_before.\n");
}

int main(int argc, char *argv[]) {
    printf("Need to implement main.\n");
    return 0;
}
